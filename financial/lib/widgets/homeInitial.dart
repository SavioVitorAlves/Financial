import 'package:flutter/material.dart';

class Homeinitial extends StatelessWidget {
  const Homeinitial({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela principal da aplicação"),
    );
  }
}
